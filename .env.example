# Required for `npm run audit:seo` — get a free key at https://serper.dev
SERPER_API_KEY=

# Set this to your domain (without https://) to override the canonical URL
# in Layout.astro. Leave blank during local development.
SITE_CUSTOM_DOMAIN=
