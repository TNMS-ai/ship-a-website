/**
 * site.config.ts — global site configuration.
 *
 * Imported by SEO audit scripts and any script that needs the brand name
 * or canonical URL. Replace the placeholder values before deploying.
 */
export const siteConfig = {
  brand:  'Your Brand',
  url:    'https://your-domain.com',
  author: 'Your Team',
} as const;
